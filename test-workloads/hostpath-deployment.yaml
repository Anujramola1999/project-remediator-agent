apiVersion: apps/v1
kind: Deployment
metadata:
  name: hostpath-test
  labels:
    app: hostpath-test
    violation: disallow-host-path
spec:
  replicas: 1
  selector:
    matchLabels:
      app: hostpath-test
  template:
    metadata:
      labels:
        app: hostpath-test
    spec:
      containers:
      - name: hostpath-container
        image: nginx:1.20
        volumeMounts:
        - name: host-volume
          mountPath: /host-data
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 200m
            memory: 256Mi
      volumes:
      - name: host-volume
        emptyDir: {}
